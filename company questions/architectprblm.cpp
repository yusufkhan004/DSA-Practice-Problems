#include <bits/stdc++.h>
#include <iomanip>
using namespace std;

int main()
{
    int b, h;
    cin >> b >> h;
    float area = (b * h) / 2;
    cout << fixed << setprecision(6) << area << endl;
    return 0;
}