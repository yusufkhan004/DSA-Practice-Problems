#include <bits/stdc++.h>
using namespace std;

int main()
{

    int days, minRange, maxRange;

    // cin >> days >> maxTemp >> minTemp;
    days = 6;
    maxRange = 34;
    minRange = 19;
    int temp[days];

    for (int i = 0; i < days; i++)
    {
        cin >> temp[i];
    }

    for (int i = 0; i < days; i++)
    {
        if(arr.find()==arr.end()){
            *arr
        }
    }

    return 0;
}