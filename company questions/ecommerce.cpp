#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<string> input;
    vector<string> temp;
    unordered_map<int, string> match;

    cin >> input;

    for (int i = 0; i < strlen(input); i++)
    {
        if (input)
    }

    return 0;
}