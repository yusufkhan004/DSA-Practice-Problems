#include <bits/stdc++.h>

using namespace std;

int main()
{
    // int num = 43251;
    // int key = 3;

    // vector<int> arr;

    // while (num != 0)
    // {
    //     int rem = num % 10;
    //     arr.push_back(rem);
    //     num = num / 10;
    // }

    // reverse(arr.begin(), arr.end());

    // for (int i : arr)
    // {
    //     cout << i << "";
    // }
    // return 0;

    // int len = arr.size();

    // for (int i = 0; i < key; i++)
    // {
    //     if (i % 2 == 0)
    //     {
    //         swap(arr[len % i], arr[len % (i + 2)]);
    //     }
    //     else
    //     {
    //         swap(arr[len % i], arr[len % (i + 2)]);
    //     }
    // }

    // for (int i : arr)
    // {
    //     cout << i << "";
    // }

    char arr[4] = "123";

    for (int i = 0; i < '\0'; i++)
    {
        cout << arr[i];
    }

    return 0;
}