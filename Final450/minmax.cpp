#include<iostream>
#include<vector>
using namespace std;

pair<int,int> minmax(vector<int> arr){

    while(arr.find(0),arr.end())
}

int main(){

    vector<int> arr = {4,2,7,9,6,0,5,1,12};

    minmax(arr);
    return 0;
}